import styled from "styled-components";

import { Img } from "@src/common/element/Img";
import { paths } from "@src/router/paths";

export const Ethel = styled(Img).attrs((props) => ({
  src: paths.public.landingDir.backroomsDir.oiiaCat,
}))``;
